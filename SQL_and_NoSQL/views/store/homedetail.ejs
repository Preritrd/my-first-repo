<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Home Details</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        main {
            max-width: 800px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .home-card {
            list-style: none;
            background-color: #fff;
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 10px;
            margin-top: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }

        .home-card img {
            width: 100%;
            max-width: 500px;
            height: auto;
            border-radius: 10px;
            display: block;
            margin: 0 auto 15px auto;
        }

        .home-card h3 {
            margin-top: 0;
            color: #2c3e50;
        }

        .home-card p {
            margin: 10px 0;
            font-size: 16px;
            color: #555;
        }

        .home-card strong {
            color: #000;
        }

        .achor-detail {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .achor-detail:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <main>
        <h1>You are in Home Details Page</h1>
        <li class="home-card">
            <h3>
                <%= home.housename %>
            </h3>
            <img src="<%= home.photoUrl %>" alt="<%= home.housename %>">

            <p><strong>Description:</strong>
                <%= home.description
                    || "A beautiful, well-furnished house perfect for families and travelers. Includes modern amenities, spacious interiors, and a scenic view from the balcony."
                    %>
            </p>
            <p><strong>Location:</strong>
                <%= home.location %>
            </p>
            <p><strong>Price:</strong> ₹<%= home.price %>
            </p>
            <p><strong>Rating:</strong>
                <%= home.rating %> ⭐
            </p>
            <%- include('../partials/favourites', { home: home }) %>
        </li>
    </main>
</body>

</html>