<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Host Home List</title>
    <style>
        /* Reset some default styles */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f9f9f9;
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        h1 {
            margin-bottom: 15px;
            color: #ff5a5f;
            font-weight: 700;
            text-align: center;
            font-size: 2.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        main {
            width: 100%;
            max-width: 1100px;
            background: #fff;
            padding: 30px 40px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        main h2 {
            font-size: 1.8rem;
            margin-bottom: 25px;
            text-align: center;
            color: #484848;
        }

        ul {
            list-style: none;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
        }

        li {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 15px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        li:hover {
            transform: translateY(-6px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
        }

        li h3 {
            font-size: 1.25rem;
            margin-bottom: 12px;
            color: #ff5a5f;
        }

        li img {
            width: 100%;
            height: 160px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 15px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        li p {
            margin-bottom: 8px;
            font-size: 0.95rem;
            color: #555;
        }

        li p strong {
            color: #333;
        }

        .rating {
            color: #ffb400;
            font-weight: 700;
        }

        .actions {
            margin-top: 15px;
            display: flex;
            justify-content: space-between;
            gap: 10px;
            width: 245px;
            padding: 1px 0;
        }

        .actions a {
            text-decoration: none;
            font-size: 20px;
            padding: 1px;
            background-color: #23419c;
            color: white;
            text-align: center;
            font-weight: 600;
            border-radius: 6px;
            transition: background-color 0.3s ease;
            box-shadow: 0 2px 6px rgba(255, 90, 95, 0.4);
        }

        .actions a:nth-child(2) {
            flex: 1;
            padding: 10px 0;
            background-color: #ff5a5f;
        }

        .actions a:hover {
            background-color: #e04e52;
            box-shadow: 0 4px 12px rgba(224, 78, 82, 0.6);
            transform: scale(1.05);
        }

        .actions a:nth-child(1):hover {
            background-color: #1e20b4;
            box-shadow: 0 4px 12px rgba(20, 55, 210, 0.6);
            transform: scale(1.05);
        }

        p.no-homes {
            text-align: center;
            font-size: 1.1rem;
            color: #888;
            padding: 40px 0;
        }

        .btn {
            padding: 10px 20px;
            width: 120px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            color: white;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .delete-btn {
            background-color: #dc3545;
        }

        .delete-btn:hover {
            background-color: #820e19;
        }

        @media (max-width: 480px) {
            main {
                padding: 20px 15px;
            }

            li img {
                height: 140px;
            }

            .actions a {
                padding: 8px 0;
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body>
    <h1>Welcome to Airbnb</h1>
    <%- include('../partials/nav') %>

    <main>
        <h2>Here Are Our Registered Homes</h2>
        <ul>
            <% if (registerhomes && registerhomes.length > 0) { %>
                <% registerhomes.forEach(home => { %>
                    <li>
                        <h3><%= home.homeName %></h3>
                        <img src="<%= home.photoUrl %>" alt="<%= home.homeName %>" />
                        <p><strong>Description:</strong> <%= home.description || "No description available." %></p>
                        <p><strong>Location:</strong> <%= home.location %></p>
                        <p><strong>Price:</strong> ₹<%= home.price %></p>
                        <p><strong>Rating:</strong> <span class="rating"><%= home.rating %> ⭐</span></p>
                        <div class="actions">
                            <a href="/edithome/<%= home._Id %>?editing=true" class="btn edit-btn">Edit</a>
                            <form action="/delete-home/<%= home._Id %>" method="POST" style="display:inline;">
                                <button type="submit" class="btn delete-btn">Delete</button>
                            </form>
                        </div>
                    </li>
                <% }) %>
            <% } else { %>
                <p class="no-homes">No homes found.</p>
            <% } %>
        </ul>
    </main>
</body>

</html>
